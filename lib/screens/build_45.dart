import 'package:flutter/material.dart';

class build45 extends StatefulWidget {
  @override
  _build45State createState() => _build45State();
}

class _build45State extends State<build45> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Gebäude 45"),
      ),
    );
  }
}
