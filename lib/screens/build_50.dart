import 'package:flutter/material.dart';

class build50 extends StatefulWidget {
  @override
  _build50State createState() => _build50State();
}

class _build50State extends State<build50> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Gebäude 50"),
      ),
    );
  }
}
